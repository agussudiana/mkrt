export abstract class ILyric {
   

    abstract recite(number: number): string;
    abstract randomRecite():string;
    abstract reciteSubject(number: number): string;
    abstract randomReciteSubject():string
  }